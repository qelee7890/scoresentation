# 개발일지

## 프로젝트 현재 상태
- 저장소는 정적 HTML/CSS/Vanilla JS 기반 프론트와 Python SQLite 서버를 함께 둘 수 있는 찬송가 프레젠테이션/편집기 프로토타입이다.
- 메인 재생 화면은 `index.html`, `app.js`, `presentation.js`, `notes.js`를 사용한다.
- 편집 화면은 `editor.html`, `editor.js`, `editor.css`, `notes.js`를 사용한다.
- 저장 계층은 `storage.js`가 담당하며, 가능하면 `server.py`의 SQLite API를 사용하고 없으면 `localStorage`로 폴백한다.
- 곡 식별은 점진적으로 `number` 중심에서 일반 `id` 중심 구조로 옮기고 있다.

## 이번까지 구현된 핵심 기능

### 프레젠테이션
- `hymns.json`에서 곡을 불러와 프레젠테이션을 렌더링한다.
- 46장은 `demo-hymn-data.js`의 데모 악보를 합쳐서 표시한다.
- 저장본이 있으면 기본 데이터보다 우선해서 불러온다.

### 가사/악보 편집기
- 편집 모드 토글 (`E`)
- 음표 생성, 박자 변경, 삭제
- 기존 음표 클릭/호버/드래그 기반 음정 변경
- 가사 직접 편집 (`contenteditable`)
- 가사 줄 분할/병합 (`Enter`, `Backspace`, `Delete`)
- `<후렴>` 마커 기반 후렴 슬라이드 전환
- 현재 절/후렴 슬라이드 추가/삭제
- 가사 수정 시 오선지 길이와 음표 위치 즉시 재계산
- 초기 슬라이드 진입 시에도 폰트 로딩 후 레이아웃 재계산
- 조표 편집
- 기존 음표 인라인 메뉴 기반 길이/점음표 편집
- 빈 오선지 드래그 + `Ctrl`/`Shift` 클릭 기반 연결선 선택
- Undo/Redo (`Ctrl+Z`, `Ctrl+Y`, `Ctrl+Shift+Z`)
- JSON 복사/다운로드
- 저장, 저장본 삭제, JSON import

## 현재 편집 규칙
- 일반 편집 모드:
  가사 위 오선지 영역을 호버하면 음표 프리뷰가 보인다.
- 일반 편집 모드:
  빈 자리 클릭으로 새 음표를 만든다.
- 일반 편집 모드:
  기존 음표 위에서 다른 높이를 가리킨 뒤 클릭하면 음정이 바뀐다.
- 일반 편집 모드:
  기존 음표를 드래그하면 음정을 바꿀 수 있다.
- 일반 편집 모드:
  더블클릭하면 해당 음표를 삭제한다.
- 조표 편집:
  음자리표 오른쪽 조표 영역 호버/클릭으로 조표를 추가한다.
- 조표 편집:
  `Ctrl`과 함께 조표 영역을 다루면 샵 기준으로 추가한다.
- 조표 편집:
  조표 영역 더블클릭으로 마지막 조표를 제거한다.
- 기존 음표 인라인 메뉴:
  `길이 변경`, `점 추가`/`점 제거`, `삭제`를 바로 실행한다.
- 연결선 선택:
  기존 8분음표/16분음표만 선택 가능하다.
- 연결선 선택:
  빈 오선지 드래그나 `Ctrl`/`Shift`+클릭으로 같은 줄의 여러 음표를 선택할 수 있다.
- 연결선 선택:
  선택은 한 줄씩만 유지한다. 다른 줄을 고르면 이전 선택은 정리된다.
- 연결선 선택:
  선택한 음표 근처 인라인 메뉴에서 `연결선 적용`, `연결선 해제`, `선택 취소`를 바로 실행한다.
- 연결선 선택:
  빈 공간 클릭 또는 `Esc`로 현재 선택을 해제한다.
- 연결선 선택:
  선택한 음표 사이에 다른 음표가 끼어 있으면 한 번에 같은 연결선 그룹으로 묶지 않는다.

## 저장/불러오기 규칙
- 현재 곡 전체 데이터를 export 대상으로 사용한다.
- export에는 가사 수정 결과와 `notes`가 함께 포함된다.
- `storage.js`는 곡 `id` 기준으로 저장본을 관리한다.
- 프레젠테이션 앱과 편집기는 같은 저장본을 공유한다.

## 최근 작업 내역

### 2026-04-12
- 조표와 첫 음표 사이 간격을 늘려 첫 음표 선택 문제를 완화했다.
- 높은음(`C6`, `D6`, `E6`)도 보조선과 함께 렌더링되도록 `notes.js`를 확장했다.
- 연결선 선택 UX를 보강했다.
- beam 선택을 한 줄 단위 정책으로 고정했다.
- 선택한 음표 근처에 인라인 연결선 메뉴를 추가했다.
- beam 모드에서 빈 공간 클릭 또는 `Esc`로 선택 해제가 가능해졌다.

### 2026-04-12 UX 재정리
- 기존 음표 위에서는 더 이상 회색 프리뷰 음표를 띄우지 않는다.
- 빈 오선지 자리에서만 새 음표 프리뷰를 표시한다.
- 기존 음표는 클릭하면 인라인 메뉴가 열리고, 드래그하면 음정을 바꾼다.
- 기존 음표 인라인 메뉴에서 `길이 변경`을 누르면 8분, 4분, 2분, 1분음표 선택 박스가 이어서 뜬다.
- 빈 오선지에서 클릭 후 드래그하면 여러 음표를 자동 선택하고 연결선 메뉴를 띄운다.
- `Ctrl` 또는 `Shift`와 함께 기존 음표를 클릭해서 연결선 선택에 추가할 수 있다.
- beam 전용 진입 모드와 상단 beam 버튼은 숨김 처리했다.

### 2026-04-13 점음표 UX 정리
- `D` 키 기반 점음표 모드 진입을 제거했다.
- 점추가는 기존 음표 인라인 메뉴 안으로 이동했다.
- 선택한 음표는 메뉴에서 `점 추가` 또는 `점 제거`를 바로 실행한다.

### 2026-04-13 백엔드 저장소 추가
- `server.py`를 추가해 정적 파일 서빙과 `/api/hymns` SQLite 저장 API를 함께 제공하도록 했다.
- `storage.js`는 서버가 있으면 DB 저장을 우선 사용하고, API가 없으면 기존 `localStorage`로 자동 폴백하도록 바꿨다.
- 기존 브라우저 `localStorage` 저장본은 서버 모드에서 더 최신 항목만 DB로 올리는 마이그레이션을 시도한다.
- `editor.js`의 저장/삭제/JSON import 흐름을 비동기 저장 API 기준으로 정리했다.
- `app.js`도 시작 시 저장소를 먼저 초기화하고 저장본을 우선 반영하도록 맞췄다.

### 2026-04-13 가사 구조 편집 추가
- 가사 줄 편집에서 `Enter`로 줄 분할, 줄 시작 `Backspace`/줄 끝 `Delete`로 줄 병합이 가능해졌다.
- 가사 줄 편집에서 `Ctrl+Enter`로 현재 커서 이후 텍스트를 같은 절/후렴 안의 새 슬라이드로 보낼 수 있게 했다.
- 한글 줄 분할/병합 시 해당 줄의 `notes` 라인 인덱스도 함께 이동하도록 정리했다.
- 절 슬라이드에 `<후렴>` 마커가 들어오면 해당 슬라이드를 `chorus` 섹션으로 옮기고 마커는 저장 텍스트에서 제거한다.
- 툴바에 슬라이드 추가/삭제 버튼을 넣어 현재 절/후렴 슬라이드를 직접 조정할 수 있게 했다.
- 내용이 있는 슬라이드를 삭제하면 같은 절/후렴의 이전 슬라이드 뒤로 내용을 이어 붙인 뒤 삭제하도록 했다.
- 마지막 남은 1개 슬라이드는 삭제하지 않도록 막았다.

### 2026-04-13 곡 식별자 일반화 시작
- 프레젠테이션/편집기/저장소가 `number` 외에 문자열 `id`도 곡 식별자로 받을 수 있게 정리했다.
- URL 쿼리는 `?song=`을 우선 사용하고, 기존 `?hymn=`도 계속 읽도록 했다.
- 저장 목록과 헤더 표시를 숫자 찬송가 전용 문구에서 일반 곡 표시로 확장했다.
- SQLite 저장 API도 숫자 전용 검증을 풀고 일반 곡 ID를 저장할 수 있게 바꿨다.
- 프레젠테이션/편집기 로더가 `hymns.json`과 `ccm.json`을 합쳐 읽는 구조로 바뀌었고, `ccm.json`이 아직 비어 있어도 기존 흐름이 유지되게 처리했다.
- `sample/꽃들도.png`를 바탕으로 `ccm-flowers` 샘플 곡 데이터를 `ccm.json`에 추가했고, 바로 열어볼 수 있는 `sample-flowers.html` 페이지도 만들었다.
- `ccm-flowers`에는 실제 가사-음표 `notes` 샘플을 넣었고, 프레젠테이션은 46장 데모뿐 아니라 `notes`가 들어 있는 곡이면 악보를 함께 렌더링하도록 바꿨다.
- 에디터 좌측 컬럼에 곡 검색 패널을 추가해 hymn/ccm 전체를 제목 또는 모든 슬라이드 가사로 검색하고, `Enter` 또는 검색 버튼으로 최대 10개의 결과를 바로 불러올 수 있게 했다.
- 에디터 하단 JSON 패널을 양방향 편집으로 바꿔서, 유효한 JSON을 수정하면 현재 곡 데이터와 슬라이드에 즉시 반영되게 했다.

## 남은 작업 후보
- 연결선 메뉴 위치를 좁은 화면에서도 더 안정적으로 보정
- Undo/Redo를 스냅샷 방식에서 명령 기반 구조로 정리
- 가사 중간 삽입/삭제 시 음표 보존 규칙 고도화
- 곡 메타데이터 편집 UI 추가
- `editor.js` 모듈 분리
- 최종 저장 포맷을 `hymns.json` 확장으로 갈지 별도 `notes.json`으로 갈지 결정

## 다음 세션 시작 체크리스트
1. `editor.html?hymn=46`에서 beam 인라인 메뉴 위치와 버튼 동작을 눈으로 확인
2. 다른 줄을 선택했을 때 beam 선택이 한 줄 정책대로 자연스럽게 바뀌는지 확인
3. 좁은 창 폭에서 beam 메뉴가 슬라이드 밖으로 나가지 않는지 확인
4. beam 메뉴를 기준으로 툴바 버튼을 유지할지 더 줄일지 결정
5. 그 다음 작업으로 Undo/Redo 구조 정리 또는 가사-음표 보존 규칙 개선 중 하나를 선택

## 참고 파일
- `editor.html`: 편집기 진입점
- `editor.js`: 편집 로직 메인
- `editor.css`: 편집기 스타일
- `notes.js`: SVG 악보 렌더러
- `app.js`: 프레젠테이션과 hymn 데이터 로더
- `storage.js`: 저장 계층 (DB 우선, 실패 시 localStorage 폴백)
- `server.py`: SQLite 저장 API + 정적 파일 서버
- `demo-hymn-data.js`: 46장 데모 악보 데이터

## 최근 반영 사항

### 2026-04-13 후반 수정
- 음표 헤드의 실제 렌더링 중심과 편집기 상호작용 좌표가 어긋나던 문제를 정리했다.
- 선택 원, 클릭 히트테스트, 음표 메뉴 위치는 `getNoteVisualPosition()` 기준으로 맞췄다.
- 새 음표 추가와 기존 음표 드래그의 pitch 판정은 다시 오선지 기준으로 분리했다.
- pitch 라벨 체계를 한 단계 낮춰 재정의했다.
  - 예: `D5 -> C5`, `C5 -> B4`, `B5 -> A5`
- `notes.js`의 `pitchMap`을 새 표기 체계에 맞게 갱신했다.
- `storage.js`, `editor.js`, `app.js`에 `pitchLabelVersion = 2` 마이그레이션을 넣어서 기존 저장본/불러오기 데이터가 한 번만 자동 변환되도록 했다.
- 보조선 규칙을 새 pitch 표기 체계에 맞게 조정했다.
  - `G5`, `B5` 상단 불필요 보조선 제거
  - `C4` 중앙 보조선 표시
  - 하단(`C4` 이하) 보조선은 반 칸 아래 보정
  - 상단(`A5` 이상) 보조선은 한 칸 위로 재조정
- 조표 변경 시 화면 상단 메타 정보도 즉시 갱신되도록 수정했다.
  - 찬송가 정보 줄의 `음조`
  - 현재 슬라이드 정보 줄의 `음조 · 박자`
- 연결선 드래그 선택의 `x` 좌표계가 SVG 내부 좌표와 섞이던 문제를 수정했다.
  - 드래그 박스 `x` 판정은 `lineEl._layout.positions[charIndex]` 기준으로 통일
- 연결선 후보 선택 규칙을 더 엄격하게 바꿨다.
  - `8분음표`와 `16분음표`는 함께 연결선 선택 불가
  - 인접하지 않은 음표를 함께 선택하려 하면 선택 자체를 막고 `"인접하지 않은 음표들이 선택되었습니다."` 경고 출력
  - 위 검증은 드래그 박스 선택과 `Ctrl`/`Shift` 클릭 추가 선택 모두에 적용

## 현재 연결선 규칙 추가 메모
- 연결선 선택은 같은 줄에서만 유지된다.
- 연결선 선택은 같은 기본 길이의 음표끼리만 가능하다.
  - `8 + 8`, `16 + 16` 가능
  - `8 + 16` 불가
- 선택 구간 사이에 다른 음표가 끼어 있으면 후보 선택 자체를 막는다.
- 잘못된 연결선 후보는 “적용 단계”가 아니라 “선택 단계”에서 바로 막는다.

## 다음 세션 시작 포인트 업데이트
1. 브라우저에서 `C4`, `A5`, `B5`, `C6` 보조선이 의도한 높이에 놓이는지 최종 확인
2. 새 pitch 라벨 체계(`v2`)가 export/import와 프레젠테이션 표시에서도 일관되게 유지되는지 확인
3. 연결선 드래그 선택에서 좁은 간격 음표를 빠르게 훑을 때 선택 감도가 과하지 않은지 확인
4. 필요하면 `개발기획서.md`에도 새 pitch 라벨 체계와 연결선 선택 규칙을 반영
