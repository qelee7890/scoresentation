# SPEC-LYRICS-001 Progress

- Started: 2026-07-11
- Branch: feature/SPEC-LYRICS-001-multilang-lyrics (base aa43aab, pushed; issue #1)
- Harness level: standard (evaluator final-pass) / development_mode: tdd
- Phase 0.5 skipped: memory_guard disabled
- Phase 0.9 complete: language = JavaScript (Electron main/renderer) + Python (tools/); no moai-lang-* skill available in project — proceeding without
- Phase 0.95 complete: Scale-based mode = Standard/Full (data-layer multi-file; manager-strategy + manager-tdd + manager-quality + evaluator-active), sub-agent mode
- Phase 1 complete: manager-strategy execution plan (10 tasks, 3 lanes, zero new devDependencies; 3-track tests: node:test pure modules / Python unittest tools / Electron smoke gate)
- Run Gate 1 (user, 2026-07-11): plan APPROVED. Decisions: contentHash = shared canonical serialization convention + golden vectors cross-verified by Python & JS; slide backfill may read 3 v1 sources read-only (json-format2 > user DB > baseline); strategy flags accepted (REQ-LYR-023 = absence-assert not physical REMOVE; coverage scope = pure JS modules + Python tools, db.js glue via smoke gate; GWT-C1 ordering via T-007←T-004)
- Phase 1.5 complete: tasks.md written (T-001~T-010, all pending)
- Phase 1.6 complete: 16 acceptance criteria registered as pending [AC-*] tasks
- Phase 1.7 note: file stubs delegated to manager-tdd first action (LSP baseline via moai-lsp unavailable in shell — using test-suite green state as baseline)
- Phase 1.8 complete: MX scan — 0 existing @MX tags in codebase (greenfield MX context; tags added for new code only per mx_plan)
- T-002 done: 11 node:test pass (canonical-doc 9 + purity-guard 2), RED (missing module import) → GREEN. files: main/canonical-doc.js, test/canonical-doc.test.js, test/purity-guard.test.js. GWT-A1/A2/A3 model shape demonstrated on hand-built fixtures.
- T-001 done: Electron characterization gate GREEN (exit 0); inverted probe confirms assertions bite (exit 1). Locks RM-E [EXISTING]: getHymn precedence (tombstone→user→baseline), saveHymn overlay-only write (baseline byte-unchanged), isNew broadcast semantics, baseline readonly+query_only rejects writes (GWT-E2). files: tools/char-existing-overlay.cjs. db.js unmodified.
- T-003 done: RED (Electron gate: saved_hymns_v3 absent) → GREEN. db.js adds saved_hymns_v3 (separate from v1), PRAGMA user_version=3 forward/idempotent/backup-first migration in _initUserSchema, getCanonicalHymn (tombstone→user v3→baseline v3, normalized via canonical-doc), baseline v3 detection. preload+main.js hymns:get-canonical read IPC. Tests: check-canonical-schema.cjs GREEN (GWT-A1 separate table, GWT-C5 idempotent), test_v3_schema.py 4 pass (schema contract). node 11 pass, char-existing still GREEN. npm test form = node --test test/*.test.js (Windows path quirk with dir arg).
- T-004 done: RED (canonical_doc/import module missing) → GREEN. tools/canonical_doc.py (Python mirror of canonical-doc.js: canonical_stringify sorted/compact/UTF-8, content_projection strips rev/updatedAt/_provenance + syllable id, compute_content_hash sha256, assign_syllable_ids). tools/import_v2_to_desktop.py (v2 mode=ro → v3 upsert, schemaVersion→3, tempo/newTitle backfill from v1 baseline, syllable ids, backup-first, WAL checkpoint(TRUNCATE), transform_hook for T-006/T-008). 6 tests pass incl. full 573 corpus (GWT-B1), idempotent identical rows/hash (GWT-B2), WAL self-contained (GWT-E1). DIVERGENCE: real repo data/scoresentation.db population deferred to end of import chain (after T-008) as one backup-first step — intermediate tests hermetic against temp fixtures.
- T-005 done: RED (default-fallback gaps=15 across 12 songs) → diagnosed (empty 0-line chorus artifacts + nested CCM json-format2 {title:{...}} structure) → GREEN. backfill_slidegroups.py: per-section highest-priority v1 source whose line-count MATCHES v2 (json-format2>user-db>baseline), C[] cumulative arithmetic → slideIndex/slideBreaks; line-count match auto-selects user-DB for #204 (amen-less). Full corpus 573/2330/7433 gap_sections=0 (GWT-B1), deterministic re-run identical hashes, #204 chorus single amen-less slide (GWT-C3 tie-in). 7 tests pass. REFACTOR: with-open (no ResourceWarning).
- T-006 done: RED (gate module missing) → GREEN, +1 calibration fix (textOnly ES lines exempt from glyph==notes — '왕이 나셨다' false positive). import_v2_to_desktop.py: apply_ko_gate (absorb line-level orphanNotes into last syllable, GLYPH_NOTE_MISMATCH warning, preserve markers — GWT-E3), es_gate_failures (3 checks: glyph==notes, letter reassembly, whitespace wbEs group vs json-format2, ported from mobile audit_es_spacing.py; benign #204 Amén residual NOT aborting), ImportGateError abort-before-write. run_import gates=opt-in; main() gates=True + non-zero exit + failing-song report. Full 573 passes gate; injected defects abort non-zero with no output (GWT-B3). 5 gate tests; full Python suite 22 pass.
